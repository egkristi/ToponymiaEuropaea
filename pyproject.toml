[project]
name = "toponymia-europaea"
version = "0.5.0"
description = "An open research framework for systematic analysis of European place names"
readme = "README.md"
license = { text = "CC-BY-NC-SA-4.0" }
requires-python = ">=3.12"
authors = [
    { name = "Erling Kristiansen" },
]
keywords = ["toponymy", "place-names", "onomastics", "geospatial", "linguistics", "NLP"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: Other/Proprietary License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: GIS",
    "Topic :: Text Processing :: Linguistic",
]

dependencies = [
    # Core
    "pydantic>=2.5",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.1",
    "geoalchemy2>=0.14",
    # Data
    "httpx>=0.27",
    # Statistics
    "numpy>=1.26",
    "scipy>=1.12",
    # CLI
    "typer>=0.9",
    "rich>=13.7",
    # Configuration
    "tomli>=2.0; python_version < '3.11'",
    "pydantic-settings>=2.1",
]

[project.optional-dependencies]
geospatial = [
    "h3>=4.0",
    "geopandas>=0.14",
    "shapely>=2.0",
    "rasterio>=1.3",
    "pyproj>=3.6",
]
nlp = [
    "spacy>=3.7",
]
warehouse = [
    "duckdb>=0.10",
    "pyarrow>=15.0",
    "statsmodels>=0.14",
]
bayesian = [
    "pymc>=5.10",
    "arviz>=0.17",
]
linkeddata = [
    "rdflib>=7.0",
]
ml = [
    "transformers>=4.37",
    "torch>=2.1",
]
api = [
    "fastapi>=0.109",
    "uvicorn[standard]>=0.27",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.1",
    "pytest-asyncio>=0.23",
    "ruff>=0.2",
    "mypy>=1.8",
    "pre-commit>=3.6",
]
docs = [
    "mkdocs>=1.5",
    "mkdocs-material>=9.5",
]

[project.scripts]
toponymia = "toponymia.cli:app"

[project.urls]
Repository = "https://github.com/egkristi/ToponymiaEuropaea"
Documentation = "https://egkristi.github.io/ToponymiaEuropaea"
Issues = "https://github.com/egkristi/ToponymiaEuropaea/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/toponymia"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "S", "B", "A", "COM", "C4", "DTZ", "ISC", "PIE", "PT", "RSE", "RET", "SIM", "TID", "TCH", "ARG", "PTH", "ERA"]
ignore = [
    "S101",    # assert usage (needed in tests)
    "COM812",  # trailing comma (conflicts with formatter)
    "ISC001",  # implicit string concat (conflicts with formatter)
    "B008",    # function call in default arg (standard Typer pattern)
    "TC003",   # move stdlib import to TYPE_CHECKING (too strict)
    "TC001",   # move third-party import to TYPE_CHECKING (too strict)
    "ARG001",  # unused function argument (CLI placeholders)
    "ARG002",  # unused method argument (ABC signatures)
    "PT028",   # pytest fixture default arg (false positive on Typer test_ commands)
]

[tool.ruff.lint.per-file-ignores]
"migrations/*" = ["E501"]  # SQL strings in migrations can be long

[tool.ruff.lint.isort]
known-first-party = ["toponymia"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["h3", "h3.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q --strict-markers"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests requiring database connection",
]

[tool.coverage.run]
source = ["src/toponymia"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.",
]

[dependency-groups]
dev = [
    "abydos>=0.5.0",
    "duckdb>=1.5.2",
    "pyarrow>=24.0.0",
]
